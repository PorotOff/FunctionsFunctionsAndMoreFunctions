<Window x:Class="FunctionsFunctionsAndMoreFunctions.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:FunctionsFunctionsAndMoreFunctions"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!-- Список функций -->
        <ListBox x:Name="FunctionList"
                 Grid.Row="0"
                 Width="200"
                 Margin="0 0 0 10"
                 ItemsSource="{Binding FunctionNames}"
                 SelectedItem="{Binding SelectedFunction}"
                 IsEnabled="True"
                 SelectionMode="Single" />

        <!-- Поля для коэффициентов a и b + выпадающий список c -->
        <StackPanel Grid.Row="1"
                    Orientation="Horizontal"
                    Margin="0 0 0 10"
                    HorizontalAlignment="Left">
            <StackPanel Margin="0 0 20 0">
                <TextBlock Text="Коэффициент a" />
                <TextBox Width="100"
                         Text="{Binding ARatio}" />
            </StackPanel>
            
            <StackPanel Margin="0 0 20 0">
                <TextBlock Text="Коэффициент b" />
                <TextBox Width="100"
                         Text="{Binding BRatio}" />
            </StackPanel>
            
            <StackPanel>
                <TextBlock Text="Коэффициент c" />
                <ComboBox Width="100"
                          ItemsSource="{Binding AvailableCValues}"
                          SelectedItem="{Binding CRatio}"
                          IsReadOnly="True"
                          IsEditable="False" />
            </StackPanel>
        </StackPanel>

        <!-- Таблица с f(x, y), x, y -->
        <DataGrid Grid.Row="2"
                  ItemsSource="{Binding DataItems}"
                  AutoGenerateColumns="False"
                  CanUserAddRows="True"
                  CanUserDeleteRows="True"
                  CanUserResizeRows="True"
                  Margin="0"
                  HeadersVisibility="Column"
                  IsReadOnly="False"
                  EnableRowVirtualization="False">
            <DataGrid.Resources>
                <Style TargetType="{x:Type DataGridRow}">
                    <Style.Resources>
                        <!-- Убираем синий фон при выделении -->
                        <SolidColorBrush x:Key="{x:Static SystemColors.HighlightBrushKey}"
                                         Color="Transparent" />
                        <!-- Убираем синий текст при выделении -->
                        <SolidColorBrush x:Key="{x:Static SystemColors.HighlightTextBrushKey}"
                                         Color="Black" />
                    </Style.Resources>
                </Style>
            </DataGrid.Resources>

            <DataGrid.Columns>
                <DataGridTextColumn Header="f(x, y)"
                                    Binding="{Binding Result}"
                                    IsReadOnly="True"
                                    Width="*" />
                <DataGridTextColumn Header="x"
                                    Binding="{Binding X}"
                                    Width="*" />
                <DataGridTextColumn Header="y"
                                    Binding="{Binding Y}"
                                    Width="*" />
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Window>
